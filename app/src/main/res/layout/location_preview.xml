<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.button.MaterialButton xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.MaterialComponents.Button"
    android:id="@+id/button_loc"
    android:background="@android:color/darker_gray"
    android:textColor="@android:color/white"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:drawableLeft="@drawable/z_height"
    android:drawableRight="@drawable/z_height"
    android:layout_marginBottom="12dp"
    app:cornerRadius="50dp"/>
    <!-- pads with drawableLeft/Right to get target height -->
    <!-- rounded corners inexplicably do not work... Android moment -->
